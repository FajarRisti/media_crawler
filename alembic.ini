[alembic]
script_location = migrations
sqlalchemy.url = driver://user:pass@localhost/dbname
prepend_sys_path = .
version_locations = %(here)s/bar %(here)s/bat migrations

[post_write_hooks]
# post_write_hooks defines scripts or Python functions that are run
# on newly generated revision scripts.  See the documentation for further
# detail and examples

# format using "black" - use the console_scripts runner, against the "black" entrypoint
hooks = black
black.type = console_scripts
black.entrypoint = black
black.options = -l 79 REVISION_SCRIPT_FILENAME

# lint with attempts to fix using the console_scripts runner against the flake8 entrypoint
#hooks = flake8
#flake8.type = console_scripts
#flake8.entrypoint = flake8
#flake8.options = --fix REVISION_SCRIPT_FILENAME

